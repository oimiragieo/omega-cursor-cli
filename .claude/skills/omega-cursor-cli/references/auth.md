# Authentication

If `cursor-agent` commands fail due to authentication, run:

```bash
cursor-agent login
```

If your environment prompts through an interactive flow, complete it once, then retry your headless command.
