# Codex-like agent surface

This repo includes `.agents/skills/omega-cursor-cli/SKILL.md` to route Codex/Codex-like workflows to the shared wrapper:

```bash
node .claude/skills/omega-cursor-cli/scripts/ask-cursor.mjs "PROMPT"
```

The canonical instructions live in `.claude/skills/omega-cursor-cli/SKILL.md`.
