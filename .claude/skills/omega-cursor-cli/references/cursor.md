# Cursor surface

Cursor rules in `.cursor/rules/` route "ask cursor" style requests to:

```bash
node .claude/skills/omega-cursor-cli/scripts/ask-cursor.mjs "PROMPT"
```

Copy `.claude` and `.cursor` together when moving this setup.
