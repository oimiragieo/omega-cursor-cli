{
  "name": "omega-cursor-cli",
  "version": "2.0.0",
  "description": "Portable agent skill for headless Cursor Agent CLI (Cursor, Codex-like agents, VS Code, Antigravity)",
  "private": true,
  "type": "module",
  "scripts": {
    "lint": "eslint .",
    "lint:fix": "eslint . --fix",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "test": "node --test tests/*.test.mjs",
    "test:ci": "npm run test && npm run lint && npm run format:check && npm run changelog:check",
    "changelog:check": "node scripts/check-changelog.mjs"
  },
  "devDependencies": {
    "@eslint/js": "^9.15.0",
    "eslint": "^9.15.0",
    "eslint-config-prettier": "^9.1.0",
    "prettier": "^3.3.3"
  },
  "overrides": {
    "minimatch": ">=10.2.3"
  }
}
