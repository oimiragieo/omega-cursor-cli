---
name: omega-cursor-cli
description: Use when a Codex/Codex-like agent should call a headless Cursor Agent CLI wrapper script for analysis, brainstorming, or machine-readable output.
---

# Omega Cursor CLI (Codex-like entry)

Delegate to the canonical instructions:

`./.claude/skills/omega-cursor-cli/SKILL.md`

Quick run:

```bash
node .claude/skills/omega-cursor-cli/scripts/ask-cursor.mjs "USER_PROMPT"
```
